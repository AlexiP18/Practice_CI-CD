{
  "name": "mi-app",
  "version": "1.0.0",
  "main": "src/index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "test": "NODE_ENV=test jest --runInBand --detectOpenHandles --verbose",
    "lint": "echo \"(opcional) ejecuta eslint\" && exit 0"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  }
}
